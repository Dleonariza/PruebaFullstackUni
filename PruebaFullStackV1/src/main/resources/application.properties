server.port=8080

# ==============================================================

# = Data Source

# ==============================================================
#jdbc:mysql://mysports.cfdr3fgqdh2v.us-east-1.rds.amazonaws.com:3306/sports?useSSL=true&serverTimezone=UT
#spring.datasource.url=jdbc:postgresql://localhost:5432/PruebaFullStack
spring.datasource.url=jdbc:postgresql://localhost:5432/ProyectPruebaSecurity
spring.datasource.username=Diego
spring.datasource.password = 123
spring.datasource.driver-class-name=org.postgresql.Driver




# ==============================================================

# = Keep the connection alive if idle for a long time (needed in production)

# ==============================================================

#spring.datasource.testWhileIdle = true

#spring.datasource.validationQuery = SELECT 1




# ==============================================================

# = Show or not log for each sql query

# ==============================================================

spring.jpa.show-sql = true




# ==============================================================

# = Hibernate ddl auto (create, create-drop, update)

# ==============================================================

spring.jpa.hibernate.ddl-auto = update

logging.level.org.hibernate.SQL = debug


# ==============================================================

# = The SQL dialect makes Hibernate generate better SQL for the chosen database

# ==============================================================
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialects
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgresSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation = true
#org.hibernate.dialect.MySQL5Dialect


security.jwt.key.private=e9eef9a1d6e4c1c223db83e75892f47eb2c833f7b9669a3d20197e104de59167
security.jwt.user.generator=AUTH0JWT-BACKEND






